language: java
addons:
    apt:
        packages:
            - libgdal-dev
            - libgdal-java
            - build-essential
            - swig
before_install:
    - jdk_switcher use oraclejdk8
    - ./scripts/install_gdal.sh
    - find $HOME/.local/lib/ || true
jdk:
    - oraclejdk8
